#include "main.h"

/**
 * _islower -> checks if the character is lowercase
 * @c: a character argument
 * Retutns: returns 0 or 1 dependes on the conditions
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
