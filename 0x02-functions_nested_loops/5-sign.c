#include "main.h"
/**
 * print_sign - ptints the sign of a number
 * @n : integer value
 * Return: returns 1 if number is > 0 ,-1 if number is < 0 & 0 if number is = 0
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
	}
	else if (n = 0)
	{
		_putchar('0')
	}
	else 
	{
		_putchar('-')
	}
}

